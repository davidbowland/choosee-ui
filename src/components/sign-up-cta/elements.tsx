import Fab from '@mui/material/Fab'
import styled from 'styled-components'

export const FloatingFab = styled(Fab)({
  bottom: 20,
  left: 'auto',
  margin: 0,
  position: 'fixed',
  right: 20,
  top: 'auto',
})
